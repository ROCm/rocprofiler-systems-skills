# Test campaign summary - 2026-05-20

First RED pass across a subset of fixtures. No GREEN runs yet; no
scenarios run yet. This file is a snapshot, not a final report.

## RED runs completed

| Fixture | Planted | Found | Notes |
|---------|---------|-------|-------|
| 01-getter-mutates | 2 | 1.5 | Polarity hit at Must Fix; getter-side-effect mis-framed as `const`-loss with `mutable` fix proposed (wrong fix that preserves the bug). **Highest-value GREEN differentiator**: must recommend rename, not `mutable`. |
| 06-python-rules | 9 | 8 (1 partial) | Mutable defaults / bare except / context manager / dead TTL caught at correct severity. Comprehension nudge missed; type hints downgraded to Nitpick. |
| 08-dead-code-and-comments | 11 | 11 (some folded) | Banner / TODO / restating / Doxygen / unreachable / commented-out all detected. Baseline added 5 legitimate bonus findings. |

## Fixtures not yet run

- 02-deep-nesting-and-name (Dim 2 / Dim 3 / Dim 4)
- 03-magic-timeout-hot-path (Agent 7 hot-path)
- 04-bare-int-on-wire (Agent 4 fixed-width)
- 05-large-pr-one-real-bug (discipline test)
- 07-cmake-rules (Agent 4 CMake)
- 09-simplify-reuse (Agent 6)
- 10-architecture-misplaced (Agent 5)

## Scenarios not yet run

All five (01-tired-shipping / 02-be-brief / 03-post-it /
04-fresh-eyes-leak / 05-re-review-misapplied).

## Skill iteration triggered by RED so far

None yet. RED proves that:
- The current SKILL.md/QUALITY.md/PERFORMANCE.md content addresses
  rules that baseline does not consistently apply (Dim 1
  side-effecting getter, Agent 6 simplify nudges, Agent 7 hot-path
  classification).
- Baseline does pick up textbook issues well; the skill's value-add
  is in semantic framing (Dim 1, hot-path, named-split proposals)
  not in re-discovering bare-except or mutable-default.

## Open questions for the next iteration

1. Run GREEN on fixture 01 - does the agent propose **rename**
   instead of `mutable`? If not, sharpen QUALITY.md Dim 1 with the
   explicit anti-pattern: "Adding `mutable` to allow a `get_*` to
   mutate IS NOT the fix. The fix is to rename the function."
2. Run GREEN on fixture 06 - does it grade type hints as Should Fix
   (per programming-python)? If it inherits the baseline's Nitpick
   grading, the language-rules-agent prompt needs an explicit
   severity table for the Python rules instead of a checklist.
3. Run scenarios 01 + 02 - do they trigger the lite-mode-gate and
   "report ALL issues" disciplines? Capture any rationalizations.

## Cost per RED run

~12-17 s wall, ~77k tokens per run on a small fixture (one C++ or
Python file, ~30-50 lines). Scales roughly linearly with file size
for the agent's read; the skill's overhead is its own SKILL.md +
the agent-prompt file (~3-5k tokens combined).
